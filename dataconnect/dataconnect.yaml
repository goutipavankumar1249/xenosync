specVersion: "v1beta"
serviceId: "login_app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "login_app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
